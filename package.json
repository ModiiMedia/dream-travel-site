{
  "name": "dream-travel-site",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "get-modules": "node importNodeModules",
    "start": "npm run get-modules && hugo server",
    "build": "npm run get-modules && hugo --minify",
    "preview:forestry": "npm run get-modules && hugo server -D -F -E --renderToDisk --port 8080 --bind 0.0.0.0"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bulma": "^0.7.5",
    "fs-extra": "^8.1.0"
  }
}
